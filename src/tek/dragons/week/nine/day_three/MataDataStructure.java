package tek.dragons.week.nine.day_three;

public class MataDataStructure {
	/*
	 * Maps are a data structure base on Key value pairs. 
	 * Map in an interface and most popular Classes that implement map.
	 * - maps are generic, for both key and value we need non-primitive data type. 
	 * - keys are based on Set
	 * - no duplicate key is accepted.
	 * - if a key is duplicate, new value will override the old one. 
	 * - In order to retrieve the value we have to know the key.   
	 * 
	 * 1. HashMap
	 * - Keys in HashMap base on HashSet
	 * 2. TreeMap
	 * - Keys in TreeMap base on TreeSet. 
	 * 
	 */

}
